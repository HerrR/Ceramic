/* global angular */

(function() {
    'use strict';

    angular
        .module('cvc')
        .controller('CvcHeaderController', Controller);

    Controller.$inject = ['$scope'];

    function Controller($scope) {
    }
})();
